@model IEnumerable<HotelPlayaParadise.Models.Intermediarios>
@using HotelPlayaParadise.Helpers
@{
    ViewBag.Title = "Index";
}
<div style="margin-top:1000px;">
    <h2>Listado de Intermediarios</h2>

    <link rel="stylesheet" href="~/css/StyleLink.css">
    <link rel="stylesheet" href="~/css/StyleTables.css">
    <link rel="stylesheet" href="~/css/StyleSummitButton.css">

    <a href="@Url.Action("Create", "Intermediarios")" class="btnG" style="text-decoration: none; color:gray; margin-top:15px">
        Crear Nuevo
    </a>
    <table class="styled-table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo_Intermediario)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tipo_Intermediario)
                </td>
                <td>
                    @Html.ActionButton("Editar", "Edit", "Intermediarios", new { id = item.Intermediario_ID }, new { @class = "btnC" }) |
                    <a href="@Url.Action("Details", "Intermediarios", new { id = item.Intermediario_ID })" class="btngreen" style="text-decoration: none; color:green">
                        Detalles
                    </a> |
                    @*@Html.ActionButton("Detalles", "Details", "CMIs", new { id = item.Id }, new { @class = "btn" })*@
                    @Html.ActionButton("Eliminar", "Delete", "Intermediarios", new { id = item.Intermediario_ID }, new { @class = "btnD" })
                </td>
            </tr>
        }

    </table>
    </div>