@model IEnumerable<HotelPlayaParadise.Models.Comentarios_Valoraciones_Clientes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valoracion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clientes.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Valoracion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clientes.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Comentario_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Comentario_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Comentario_ID })
        </td>
    </tr>
}

</table>
